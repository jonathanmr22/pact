# =============================================================================
# Reusable Solutions Knowledge Base
#
# CHECK THIS BEFORE DEBUGGING. A previous session may have already solved
# your problem. Search by tags.
#
# Format: Each entry describes a class of bug and its canonical fix.
# These are patterns, not specific instances.
# =============================================================================

solutions:

  # - id: SOL-001
  #   title: "Provider cache not updated on delete"
  #   tags: [stale-cache, provider]
  #   symptom: "Entity deleted from database but still shows in UI"
  #   root_cause: "Provider's delete method updated the List but not the Map cache"
  #   fix: "Update BOTH _entityList and _entityById in every add/update/delete method"
  #   prevention: "Cognitive redirection: always check both caches"
  #   applies_to: ["Any provider with dual List + Map caches"]

  # - id: SOL-002
  #   title: "setState called on disposed widget"
  #   tags: [async-safety, lifecycle]
  #   symptom: "Red screen or silent failure after navigating away during async operation"
  #   root_cause: "Await completes after widget disposal, setState hits dead context"
  #   fix: "Add 'if (!mounted) return;' after every await before setState/Navigator"
  #   prevention: "PostToolUse hook warns when pattern detected"
  #   applies_to: ["Any StatefulWidget with async operations"]
