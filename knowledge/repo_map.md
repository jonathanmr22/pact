# Repo Map

_Auto-generated by [scripts/repo_map.py](scripts/repo_map.py) — DO NOT EDIT._
_Generated: 2026-05-01 00:26 Central Daylight Time • files scanned: 19 • token budget: 2000_

This map shows the most central source files by import-graph PageRank (personalized by recent edits). Each section lists the public symbols the file defines. **Read this before declaring a feature 'doesn't exist'.**

---

## Script (1)

### scripts/repo_map.py  _(score 0.0526)_

- `function _find_dashboard_dir` — `def _find_dashboard_dir(root: Path) -> Path` (L82)
- `class Symbol` — `class Symbol` (L178)
- `class FileInfo` — `class FileInfo` (L191)
- `function is_excluded` — `def is_excluded(path: Path) -> bool` (L200)
- `function discover_files` — `def discover_files() -> list[Path]` (L208)
- `function get_node_name` — `def get_node_name(node) -> Optional[str]` (L230)
- `function _extract_leading_doc` — `def _extract_leading_doc(src_lines: list[bytes], line_idx: int, lang: str) -> str` (L257)
- `function extract_symbols` — `def extract_symbols(root, src: bytes, lang: str, include_private: bool) -> list[Symbol]` (L363)
- _… +50 more symbols_

## Other (18)

### templates/dashboard/pact-server.py  _(score 0.0526)_

_flows: dashboard_yaml_sync_

- `class DashHandler` — `class DashHandler(http.server.SimpleHTTPRequestHandler)` (L35)
- `function __init__` — `def __init__(self, *args, **kwargs)` (L38)
- `function _resolve_project_root` — `def _resolve_project_root(self)` (L47)
- `function _serve_yaml_from` — `def _serve_yaml_from(self, abs_path: Path)` (L63)
- `function _resolve_dashboard_dir` — `def _resolve_dashboard_dir(self, project_root: Path) -> Path` (L81)
- `function do_GET` — `def do_GET(self)` (L91)
- `function do_POST` — `def do_POST(self)` (L106)
- `function _project_root_for_notes` — `def _project_root_for_notes(self)` (L130)
- _… +14 more symbols_

### templates/delegation/generate_roster_cards.py  _(score 0.0526)_

- `function get_font` — `def get_font(size, bold=False)` (L30)
- `function get_title_font` — `def get_title_font(size)` (L43)
- `function hex_to_rgb` — `def hex_to_rgb(hex_str)` (L56)
- `function draw_stat_bar` — `def draw_stat_bar(draw, x, y, width, value, color, label, fonts)` (L62)
- `function draw_glow_circle` — `def draw_glow_circle(img, center, radius, color, alpha=40)` (L86)
- `function fetch_logo` — `def fetch_logo(url, size=(120, 120))` (L100)
- `function draw_role_icon` — `def draw_role_icon(draw, center, role, color, size=60)` (L116)
- `function generate_card` — `def generate_card(name, full_name, tagline, role, color_hex, accent_hex,` (L147)
- _… +2 more symbols_

### templates/memory/pact-feedback-report.py  _(score 0.0526)_

- `function load_events` — `def load_events()` (L28)
- `function load_feedback` — `def load_feedback(project_root=None)` (L46)
- `function generate` — `def generate(project_root=None)` (L67)
- `function mark_done` — `def mark_done(milestone)` (L205)
- `function main` — `def main()` (L217)

### templates/memory/pact-memory.py  _(score 0.0526)_

- `function get_model_dir` — `def get_model_dir()` (L50)
- `function embed` — `def embed(texts)` (L78)
- `function serialize_f32` — `def serialize_f32(vector)` (L115)
- `function get_db` — `def get_db()` (L120)
- `function store` — `def store(doc_id, doc_type, text, file=None, project=None, metadata=None)` (L147)
- `function query` — `def query(text, top_k=5, doc_type=None, project=None)` (L172)
- `function stats` — `def stats()` (L225)
- `function reindex` — `def reindex(project_root)` (L235)
- _… +2 more symbols_

### templates/memory/pact-migrate.py  _(score 0.0526)_

- `function main` — `def main()` (L27)

### templates/scripts/regenerate_plan_index.py  _(score 0.0526)_

- `function extract_meta` — `def extract_meta(path: Path)` (L32)
- `function main` — `def main()` (L60)

### templates/scripts/repo_map.py  _(score 0.0526)_

_flows: repo_map_pipeline_

- `function _find_dashboard_dir` — `def _find_dashboard_dir(root: Path) -> Path` (L82)
- `class Symbol` — `class Symbol` (L174)
- `class FileInfo` — `class FileInfo` (L187)
- `function is_excluded` — `def is_excluded(path: Path) -> bool` (L196)
- `function discover_files` — `def discover_files() -> list[Path]` (L204)
- `function get_node_name` — `def get_node_name(node) -> Optional[str]` (L226)
- `function _extract_leading_doc` — `def _extract_leading_doc(src_lines: list[bytes], line_idx: int, lang: str) -> str` (L253)
- `function extract_symbols` — `def extract_symbols(root, src: bytes, lang: str, include_private: bool) -> list[Symbol]` (L359)
- _… +50 more symbols_

### templates/scripts/verify_feature_flow_schema.py  _(score 0.0526)_

_flows: repo_map_pipeline_

- `class Violation` — `class Violation` (L73)
- `class FlowReport` — `class FlowReport` (L82)
- `function load_repo_map` — `def load_repo_map() -> dict` (L88)
- `function load_flow` — `def load_flow(path: Path) -> Optional[dict]` (L97)
- `function discover_flows` — `def discover_flows() -> list[Path]` (L107)
- `function flow_name_from_path` — `def flow_name_from_path(path: Path) -> str` (L113)
- `function index_repo_map` — `def index_repo_map(repo_map: dict) -> tuple[set[str], dict[str, set[str]]]` (L117)
- `function index_flow_files` — `def index_flow_files() -> dict[str, str]` (L137)
- _… +4 more symbols_

### templates/hooks/lib/brag_scoring.py  _(score 0.0526)_

_flows: cognitive_redirect_

- `function detect_user_positive_reaction` — `def detect_user_positive_reaction(` (L75)
- `function compute_novelty` — `def compute_novelty(history_outcomes: list[dict], pattern_id: str,` (L146)
- `function detect_bug_avoidance` — `def detect_bug_avoidance(category: str, fire_ts: str,` (L181)
- `function compute_brag_score` — `def compute_brag_score(` (L237)
- `function main` — `def main()` (L332)

### templates/hooks/lib/detect_self_correction.py  _(score 0.0526)_

_flows: cognitive_redirect_

- `function _read_path_re` — `def _read_path_re(pattern: str)` (L70)
- `function pred` — `def pred(input_dict: dict) -> bool` (L73)
- `function _bash_command_re` — `def _bash_command_re(pattern: str)` (L79)
- `function pred` — `def pred(input_dict: dict) -> bool` (L81)
- `function _any_input` — `def _any_input(input_dict: dict) -> bool` (L87)
- `function _load_session_records` — `def _load_session_records(session_file: Path) -> list[dict]` (L227)
- `function _records_to_assistant_turns` — `def _records_to_assistant_turns(records: list[dict]) -> list[dict]` (L245)
- `function _flush` — `def _flush()` (L262)
- _… +8 more symbols_

### templates/hooks/lib/inject_brag_citation.py  _(score 0.0526)_

_flows: cognitive_redirect_

- `function _load_brags_for_pattern` — `def _load_brags_for_pattern(outcomes_path: Path, pattern_id: str) -> list[dict]` (L30)
- `function build_brag_citation` — `def build_brag_citation(pattern_id: str, outcomes_path: Path) -> str` (L55)
- `function _describe_signal` — `def _describe_signal(signal: str | None) -> str` (L118)
- `function main` — `def main()` (L136)

### templates/hooks/lib/learn_triggers.py  _(score 0.0526)_

_flows: cognitive_redirect_

- `function extract_phrases` — `def extract_phrases(text: str, ngram_range: tuple[int, int] = (2, 6)) -> list[str]` (L108)
- `function walk_sessions` — `def walk_sessions(projects_root: Path, max_sessions: int | None = None) -> list[Path]` (L133)
- `function extract_assistant_text` — `def extract_assistant_text(record: dict) -> str` (L147)
- `function extract_user_text` — `def extract_user_text(record: dict) -> str` (L167)
- `function mine_session` — `def mine_session(session_file: Path) -> tuple[list[tuple[str, str]], int, int]` (L182)
- `function main` — `def main()` (L229)

### templates/hooks/lib/scan_triggers.py  _(score 0.0526)_

_flows: cognitive_redirect_

- `class Match` — `class Match` (L42)
- `function load_triggers` — `def load_triggers(triggers_path: str | Path) -> dict[str, Any]` (L51)
- `function _strip_use_mention_contexts` — `def _strip_use_mention_contexts(text: str) -> str` (L63)
- _… +3 more symbols_


---
_Truncated to fit budget. 5 of 19 files not shown — run `python scripts/repo_map.py build --budget 5000` to expand._
